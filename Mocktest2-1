stack = []

def push(item):
    stack.append(item)

def pop():
    if not is_empty():
        return stack.pop()
    else:
        raise IndexError("Stack is empty")

def is_empty():
    return len(stack) == 0
